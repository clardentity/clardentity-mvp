[pytest]
testpaths = tests
# Every async test in this suite is a plain coroutine; opting in per-test with
# a marker is ceremony when that is true of all of them.
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning
